/* Connor Collins made this. */(function() {
    require.config({
        paths: {
            jquery: "libs/jquery.min",
            mapbox: "//api.tiles.mapbox.com/mapbox.js/v1.6.4/mapbox"
        }
    }), require([ "jquery", "mapbox", "modules/site.min", "modules/map.min" ], function(jquery, mapbox, site, map) {
        return "function" == typeof define && define.amd && define.amd.jQuery && define("jquery", [], function() {
            return jQuery;
        }), site.init(), map = map.init("map_wrapper"), null !== map ? console.log("MAP IS ALIVE!") : void 0;
    });
}).call(this);